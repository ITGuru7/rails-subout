development:
  sessions:
    default:
      database: subout_development
      hosts:
        - localhost:27017
      options:
        consistency: :strong
  options:
production:
  sessions:
    default:
      database: subout_production
    options:
      skip_version_check: true
      safe: true
