%h2 Settings
%table.table.table-stripe.table-bordered
  %tr
    %th Key
    %th Value
    %th
  - Setting.all.each do |setting|
    %tr
      %td= setting.to_s
      %td= setting.value
      %td= link_to "Edit", edit_admin_setting_path(setting), class: 'btn btn-primary'

